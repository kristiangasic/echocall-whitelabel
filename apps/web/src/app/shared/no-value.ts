/**
 * What a table cell shows where the portal has nothing to put in it. On a
 * phone every cell carries its own label, so an empty one would leave the
 * label standing on its own as if the card had lost a line.
 */
export const NO_VALUE = '\u2013';
